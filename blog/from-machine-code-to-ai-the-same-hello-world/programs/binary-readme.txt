# Superseded note (kept for git history readers)
# ==============================================
# The Absolute machine program band no longer centers on a fat ELF dump.
# Prefer:
#   absolute-words.txt  - instruction/data words people would have entered
#   tabulate-words.txt  - period-typical sum 1..10 job
#   elf-note.txt        - modern OS packaging (crate, not invoice)
#
# Run Program on those carousel slides executes a minimal no-libc binary
# (sys_write / sys_exit or the numerical total), not "the ELF you ship."
